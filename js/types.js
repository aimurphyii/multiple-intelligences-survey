// 'use-strict'

// // If there is time and anybody wants to take a shake at it this si a great opportunity to do another js manipulation, to generate the types list, even though it isn't necessary. It's just another opportunity to practice :)

// // create a constructor

// function IqType (name, blurb, image, ){
//   this.name = name;
//   this. blurb = blurb;
//   this. image = image;
//   this.id = id;
// }

// // make new type objects
// var spatialIQ = new IqType ('spatial', 'Spatial intelligence involves the potential to recognize and use the patterns of wide space and more confined areas', 'img/img.jpg', '#spatial');

// // push them into list